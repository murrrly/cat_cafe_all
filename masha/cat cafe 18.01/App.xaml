<Application x:Class="RPM.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="LoginWindow.xaml"
			 xmlns:conv="clr-namespace:RPM.Converters"
>
	<Application.Resources>
		<conv:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter"/>
		
		<DropShadowEffect x:Key="DropShadowEffect" Color="#888" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
		<Style TargetType="{x:Type Control}">
			<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
		</Style>

		<!-- ItemsControl (карточки заказов) -->
		<Style TargetType="ItemsControl">
			<Setter Property="Focusable" Value="False"/>
		</Style>

		<!-- ContentPresenter  -->
		<Style TargetType="ContentPresenter">
			<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
			<Setter Property="SnapsToDevicePixels" Value="True"/>
		</Style>

		<!-- ScrollViewer тоже ловит фокус -->
		<Style TargetType="ScrollViewer">
			<Setter Property="Focusable" Value="False"/>
			<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
		</Style>

		<!-- TextBlock на всякий -->
		<Style TargetType="TextBlock">
			<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
		</Style>
		<!-- Дефолтный стиль для всех ComboBox -->
		<Style TargetType="ComboBox">
			<Setter Property="Foreground" Value="#333"/>
			<Setter Property="Background" Value="White"/>
			<Setter Property="BorderBrush" Value="#C58C5A"/>
			<Setter Property="BorderThickness" Value="1.5"/>
			<Setter Property="Padding" Value="6,3"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="Width" Value="220"/>
			<Setter Property="Height" Value="36"/>
			<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
			<Setter Property="IsEditable" Value="False"/>
			<!-- Важно для корректного выбора -->
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="ComboBox">
						<Grid>
							<Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="12"
                                    SnapsToDevicePixels="True"
                                    Effect="{StaticResource DropShadowEffect}"/>

							<DockPanel Margin="2">
								<ContentPresenter x:Name="ContentSite"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  Margin="10,0"/>

								<ToggleButton DockPanel.Dock="Right"
											  Focusable="False"
											  IsTabStop="False"
											  FocusVisualStyle="{x:Null}"
											  Background="Transparent"
											  BorderBrush="Transparent"
											  Width="180"
											  Height="24"
											  IsChecked="{Binding Path=IsDropDownOpen, 
											  RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
											  ClickMode="Press">
									<ToggleButton.Template>
										<ControlTemplate TargetType="ToggleButton">
											<Border Background="Transparent" BorderBrush="Transparent" BorderThickness="0">
												<ContentPresenter HorizontalAlignment="Center"
																  VerticalAlignment="Center"/>
											</Border>
										</ControlTemplate>
									</ToggleButton.Template>
								</ToggleButton>
							</DockPanel>

							<Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
								<Grid Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="250">
									<Border x:Name="DropDownBorder"
                                            Background="White"
                                            BorderBrush="#C58C5A"
                                            BorderThickness="2"
                                            CornerRadius="12"
                                            Effect="{StaticResource DropShadowEffect}"/>
									<ScrollViewer Margin="3" 
												  Padding="3" SnapsToDevicePixels="True"
                                                  Focusable="False"
                                                  KeyboardNavigation.DirectionalNavigation="Contained">
										<StackPanel IsItemsHost="True"/>
									</ScrollViewer>
								</Grid>
							</Popup>
						</Grid>

						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="Border" Property="Background" Value="#FFF4E1"/>
							</Trigger>
							<Trigger Property="IsEnabled" Value="False">
								<Setter TargetName="Border" Property="Background" Value="#EEE"/>
								<Setter Property="Foreground" Value="#AAA"/>
							</Trigger>
							<Trigger Property="IsDropDownOpen" Value="True">
								<Setter TargetName="Border" Property="Background" Value="#FFF4E1"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style TargetType="ComboBoxItem">
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="Foreground" Value="#333"/>
			<Setter Property="Padding" Value="14,8"/>
			<Setter Property="MinHeight" Value="30"/>
			<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
			<Setter Property="VerticalContentAlignment" Value="Center"/>
			<Setter Property="FocusVisualStyle" Value="{x:Null}"/>

			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="ComboBoxItem">
						<Border x:Name="Bd"
								Background="{TemplateBinding Background}"
								CornerRadius="10"
								Padding="{TemplateBinding Padding}"
								MinHeight="{TemplateBinding MinHeight}">

							<ContentPresenter
									VerticalAlignment="Center"
									HorizontalAlignment="Left"/>
						</Border>

						<ControlTemplate.Triggers>

							<!-- Наведение -->
							<Trigger Property="IsHighlighted" Value="True">
								<Setter TargetName="Bd"
                                Property="Background"
                                Value="#FFF4E1"/>
							</Trigger>

							<!-- Выбранный -->
							<Trigger Property="IsSelected" Value="True">
								<Setter TargetName="Bd"
                                Property="Background"
                                Value="#FFD8A86B"/>
								<Setter Property="Foreground" Value="#000"/>
							</Trigger>

							<!-- Выбран + наведение -->
							<MultiTrigger>
								<MultiTrigger.Conditions>
									<Condition Property="IsSelected" Value="True"/>
									<Condition Property="IsHighlighted" Value="True"/>
								</MultiTrigger.Conditions>
								<Setter TargetName="Bd"
										Property="Background"
										Value="#FFD29A4D"/>
							</MultiTrigger>

						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>




		<!-- Глобальный стиль для всех кнопок -->
		<Style TargetType="Button">
			<Setter Property="Background" Value="#FFF1D8"/>
			<Setter Property="BorderBrush" Value="Transparent"/>
			<Setter Property="FontFamily" Value="Inter"/>
			<Setter Property="FontWeight" Value="Medium"/>
			<Setter Property="FontSize" Value="16"/>
			<Setter Property="HorizontalContentAlignment" Value="Center"/>
			<!-- по центру горизонтали -->
			<Setter Property="VerticalContentAlignment" Value="Center"/>
			<!-- по центру вертикали -->
			<Setter Property="Height" Value="40"/>
			<Setter Property="Margin" Value="0,0,0,8"/>
			<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="8">
							<ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsFocused" Value="True">
								<Setter TargetName="ButtonBorder" Property="Background" Value="#FFFFFF"/>
								<Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#E84C34"/>
								<Setter TargetName="ButtonBorder" Property="BorderThickness" Value="2"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
		<!-- Убираем системный синий highlight во всём приложении -->
		<SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FFD8A86B"/>
		<SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FFD8A86B"/>

		<SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#000000"/>
		<SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#000000"/>

		<SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#FFE8C2"/>
		<SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#000000"/>

	</Application.Resources>

</Application>

