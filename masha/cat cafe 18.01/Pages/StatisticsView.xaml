<UserControl x:Class="RPM.Pages.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

	<Grid Margin="20">
		<StackPanel>
			<TextBlock Text="Статистика" FontSize="28" Margin="0,0,0,20"/>

			<StackPanel Orientation="Horizontal" Margin="0,0,0,20" HorizontalAlignment="Left">
				<StackPanel Margin="0,0,20,0">
					<TextBlock Text="Выручка за день:" FontWeight="Bold"/>
					<TextBlock x:Name="RevenueDayText" FontSize="16"/>
				</StackPanel>
				<StackPanel Margin="0,0,20,0">
					<TextBlock Text="Выручка за месяц:" FontWeight="Bold"/>
					<TextBlock x:Name="RevenueMonthText" FontSize="16"/>
				</StackPanel>
				<StackPanel>
					<TextBlock Text="Количество заказов:" FontWeight="Bold"/>
					<TextBlock x:Name="OrdersCountText" FontSize="16"/>
				</StackPanel>
			</StackPanel>

			<TextBlock Text="Топ блюд:" FontWeight="Bold" Margin="0,0,0,10"/>
			<DataGrid x:Name="TopMenuDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" Height="350" IsReadOnly="True">
				<DataGrid.Columns>
					<DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
					<DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="100"/>
				</DataGrid.Columns>
			</DataGrid>

			<Button Content="Обновить" Width="120" Height="35" Margin="0,10,0,0" HorizontalAlignment="Left" Click="RefreshButton_Click"/>
		</StackPanel>
	</Grid>
</UserControl>
