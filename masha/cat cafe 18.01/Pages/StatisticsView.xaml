<UserControl x:Class="RPM.Pages.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FFF9F2">

	<UserControl.Resources>
		<!-- Градиенты для карточек -->
		<LinearGradientBrush x:Key="RevenueDayGradient" StartPoint="0,0" EndPoint="1,1">
			<GradientStop Color="#FFF4E1" Offset="0"/>
			<GradientStop Color="#FFD8A86B" Offset="1"/>
		</LinearGradientBrush>
		<LinearGradientBrush x:Key="RevenueMonthGradient" StartPoint="0,0" EndPoint="1,1">
			<GradientStop Color="#FFE8C2" Offset="0"/>
			<GradientStop Color="#FFD8A86B" Offset="1"/>
		</LinearGradientBrush>

		<!-- Выравнивание чисел в DataGrid -->
		<Style x:Key="CenteredTextStyle" TargetType="TextBlock">
			<Setter Property="HorizontalAlignment" Value="Center"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
		</Style>
	</UserControl.Resources>

	<Grid Margin="40, 40, 40, 10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Заголовок -->
		<TextBlock Text="Статистика CatCafe"
                   FontSize="32"
                   FontWeight="Bold"
                   Foreground="#3A2A1F"/>

		<!-- Фильтр по филиалам -->
		<StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,60,0,0">
			<TextBlock Text="Филиал:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
			<ComboBox x:Name="BranchFilterComboBox"
                  Grid.Row="1"
                  Width="250"
                  Height="36"
                  SelectionChanged="BranchFilterComboBox_SelectionChanged"/>

		</StackPanel>

		<!-- KPI карточки -->
		<UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,0">

			<!-- Выручка сегодня -->
			<Border CornerRadius="20" Padding="25" Margin="12" Background="{StaticResource RevenueDayGradient}" 
                    Effect="{StaticResource DropShadowEffect}">
				<StackPanel>
					<TextBlock Text="Выручка сегодня" FontSize="14" Foreground="#5A4632"/>
					<TextBlock x:Name="RevenueDayText" FontSize="28" FontWeight="Bold" Foreground="#3A2A1F" Margin="0,6,0,0"/>
				</StackPanel>
			</Border>

			<!-- Выручка за месяц -->
			<Border CornerRadius="20" Padding="25" Margin="12" Background="{StaticResource RevenueMonthGradient}" 
                    Effect="{StaticResource DropShadowEffect}">
				<StackPanel>
					<TextBlock Text="Выручка за месяц" FontSize="14" Foreground="#5A4632"/>
					<TextBlock x:Name="RevenueMonthText" FontSize="28" FontWeight="Bold" Foreground="#3A2A1F" Margin="0,6,0,0"/>
				</StackPanel>
			</Border>

			<!-- Количество заказов -->
			<Border CornerRadius="20" Padding="25" Margin="12" Background="White" BorderBrush="#C58C5A" BorderThickness="1.5"
                    Effect="{StaticResource DropShadowEffect}">
				<StackPanel>
					<TextBlock Text="Количество заказов" FontSize="14" Foreground="#5A4632"/>
					<TextBlock x:Name="OrdersCountText" FontSize="28" FontWeight="Bold" Foreground="#3A2A1F" Margin="0,6,0,0"/>
				</StackPanel>
			</Border>

			<!-- Средний чек -->
			<Border CornerRadius="20" Padding="25" Margin="12" Background="#FFF4E1" Effect="{StaticResource DropShadowEffect}">
				<StackPanel>
					<TextBlock Text="Средний чек" FontSize="14" Foreground="#5A4632"/>
					<TextBlock x:Name="AverageCheckText" FontSize="28" FontWeight="Bold" Foreground="#3A2A1F" Margin="0,6,0,0"/>
				</StackPanel>
			</Border>

		</UniformGrid>

		<!-- Нижний блок -->
		<Grid Grid.Row="2">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			

			<!-- Топ блюд -->
					<Border Grid.Column="0" Background="White" CornerRadius="20" Padding="25" Margin="12" Effect="{StaticResource DropShadowEffect}">
						<Grid>
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto"/>
								<!-- Заголовок -->
								<RowDefinition Height="*"/>
								<!-- Таблица с прокруткой -->
								<RowDefinition Height="Auto"/>
								<!-- Кнопка -->
							</Grid.RowDefinitions>

							<!-- Заголовок -->
							<TextBlock Grid.Row="0" Text="Топ блюд" FontSize="18" FontWeight="SemiBold" Foreground="#3A2A1F" Margin="0,0,0,15"/>

							<!-- Прокручиваемая таблица -->
							<DataGrid Grid.Row="1"
							  x:Name="TopMenuDataGrid"
							  AutoGenerateColumns="False"
							  CanUserAddRows="False"
							  IsReadOnly="True"
							  HeadersVisibility="Column"
							  RowBackground="White"
							  AlternatingRowBackground="#FFF9F2"
							  BorderThickness="0"
							  FontSize="14"
							  Foreground="#3A2A1F"
							  ScrollViewer.VerticalScrollBarVisibility="Auto">

								<DataGrid.ColumnHeaderStyle>
									<Style TargetType="DataGridColumnHeader">
										<Setter Property="Background">
											<Setter.Value>
												<LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
													<GradientStop Color="#FFD8A86B" Offset="0"/>
													<GradientStop Color="#FFF4E1" Offset="1"/>
												</LinearGradientBrush>
											</Setter.Value>
										</Setter>
										<Setter Property="Foreground" Value="#3A2A1F"/>
										<Setter Property="FontWeight" Value="Bold"/>
										<Setter Property="Height" Value="40"/>
										<Setter Property="HorizontalContentAlignment" Value="Center"/>
										<Setter Property="VerticalContentAlignment" Value="Center"/>
									</Style>
								</DataGrid.ColumnHeaderStyle>

								<DataGrid.Columns>
									<DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
									<DataGridTextColumn Header="Продано" Binding="{Binding Quantity}" Width="100" ElementStyle="{StaticResource CenteredTextStyle}"/>
								</DataGrid.Columns>
							</DataGrid>

							<!-- Кнопка обновить -->
							<Button Grid.Row="2" Content="Обновить" Width="150" Margin="0,20,0,0" HorizontalAlignment="Left" Click="RefreshButton_Click"/>
						</Grid>
					</Border>

		</Grid>
	</Grid>
</UserControl>
