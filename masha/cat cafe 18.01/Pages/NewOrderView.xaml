<UserControl x:Class="RPM.Pages.NewOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

	<Grid Margin="20">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="2*"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<!-- ЛЕВОЕ: Меню -->
		<StackPanel Grid.Column="0">
			<TextBlock Text="Меню" FontSize="24" Margin="0,0,0,10"/>
			<DataGrid x:Name="MenuDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single" Height="400">
				<DataGrid.Columns>
					<DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
					<DataGridTextColumn Header="Состав" Binding="{Binding Composition}" Width="200"/>
					<DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="100"/>
				</DataGrid.Columns>
			</DataGrid>
			<Button Content="Добавить в корзину" Width="150" Height="35" Margin="0,10,0,0"
                    Click="AddToCart_Click"/>
		</StackPanel>

		<!-- ПРАВОЕ: Корзина и оплата -->
		<StackPanel Grid.Column="1" Margin="10,0,0,0">
			<TextBlock Text="Корзина" FontSize="24" Margin="0,0,0,10"/>
			<DataGrid x:Name="CartDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" Height="250">
				<DataGrid.Columns>
					<DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
					<DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="60"/>
					<DataGridTextColumn Header="Цена" Binding="{Binding TotalPrice}" Width="80"/>
				</DataGrid.Columns>
			</DataGrid>

			<TextBlock x:Name="TotalText" Text="Итого: 0" FontSize="20" Margin="0,10,0,0"/>

			<TextBlock Text="Тип оплаты:" FontSize="16" Margin="0,20,0,5"/>
			<ComboBox x:Name="PaymentTypeCombo" SelectedIndex="0">
				<ComboBoxItem Content="Наличные"/>
				<ComboBoxItem Content="Карта"/>
			</ComboBox>

			<Button Content="Оформить" Width="150" Height="40" Margin="0,20,0,0"
                    Click="PlaceOrder_Click"/>
		</StackPanel>
	</Grid>
</UserControl>
