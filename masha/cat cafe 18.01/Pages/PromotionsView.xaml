<UserControl x:Class="RPM.Pages.PromotionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

	<Grid Margin="20">
		<StackPanel>
			<TextBlock Text="Акции" FontSize="28" Margin="0,0,0,20"/>

			<StackPanel Orientation="Horizontal" Margin="0,0,0,10">
				<Button Content="Создать" Width="100" Height="35" Click="CreateButton_Click" Margin="0,0,10,0"/>
			</StackPanel>

			<DataGrid x:Name="PromotionsDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" Height="400" IsReadOnly="True">
				<DataGrid.Columns>
					<DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
					<DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="200"/>
					<DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
					<DataGridTextColumn Header="Начало" Binding="{Binding StartDate}" Width="100"/>
					<DataGridTextColumn Header="Конец" Binding="{Binding EndDate}" Width="100"/>
					<DataGridTextColumn Header="Цена по акции" Binding="{Binding DiscountPrice}" Width="100"/>
				</DataGrid.Columns>
			</DataGrid>
		</StackPanel>
	</Grid>
</UserControl>
