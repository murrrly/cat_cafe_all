<UserControl x:Class="RPM.Pages.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

	<Grid Margin="20">
		<StackPanel>
			<TextBlock Text="Список заказов" FontSize="28" Margin="0,0,0,20"/>

			<Button Content="Обновить" Width="120" Height="40" 
                    HorizontalAlignment="Left" Click="RefreshButton_Click" Margin="0,0,0,10"/>

			<DataGrid x:Name="OrdersDataGrid" AutoGenerateColumns="False" CanUserAddRows="False"
                      IsReadOnly="True">
				<DataGrid.Columns>
					<DataGridTextColumn Header="Номер" Binding="{Binding OrderNumber}" Width="150"/>
					<DataGridTextColumn Header="Дата" Binding="{Binding OrderDate}" Width="200"/>
					<DataGridTextColumn Header="Сумма" Binding="{Binding TotalAmount}" Width="100"/>
					<DataGridTextColumn Header="Кассир" Binding="{Binding CashierName}" Width="*"/>
				</DataGrid.Columns>
			</DataGrid>
		</StackPanel>
	</Grid>
</UserControl>
