<Page x:Class="RPM.Pages.MenuManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Меню">

	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Заголовок -->
		<TextBlock Text="🍽 Управление меню"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

		<!-- Фильтры -->
		<Border Grid.Row="1" Padding="10" Background="#FFF5F5F5" CornerRadius="8" Margin="0,0,0,15">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="2*"/>
					<ColumnDefinition Width="1*"/>
					<ColumnDefinition Width="1*"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>

				<TextBox x:Name="SearchBox" Height="34"
                         VerticalContentAlignment="Center"
                         TextChanged="SearchBox_TextChanged"/>

				<ComboBox x:Name="CategoryBox" Grid.Column="1" Margin="10,0"
                          SelectionChanged="CategoryBox_SelectionChanged"/>

				<CheckBox x:Name="AvailableCheck" Grid.Column="2"
                          Content="Только доступные"
                          VerticalAlignment="Center"
                          Checked="AvailableCheck_Changed"
                          Unchecked="AvailableCheck_Changed"/>

				<Button Grid.Column="3"
                        Content="➕ Добавить"
                        Padding="12,5"
                        Click="AddDish_Click"/>
			</Grid>
		</Border>

		<!-- Таблица -->
		<DataGrid Grid.Row="2"
                  x:Name="MenuGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
			<DataGrid.Columns>
				<DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
				<DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
				<DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="150"/>
				<DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="100"/>
				<DataGridCheckBoxColumn Header="Доступно" Binding="{Binding IsAvailable}" Width="100"/>
				<DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="200"/>
				<DataGridTemplateColumn Header="⚙" Width="120">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal">
								<Button Content="✏" Click="Edit_Click" Margin="2"/>
								<Button Content="🗑" Click="Delete_Click" Margin="2"/>
							</StackPanel>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</Page>
