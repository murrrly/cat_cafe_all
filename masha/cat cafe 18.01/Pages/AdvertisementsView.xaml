<UserControl x:Class="RPM.Pages.AdvertisementsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

	<Grid Margin="20">
		<StackPanel>
			<TextBlock Text="Реклама" FontSize="28" Margin="0,0,0,20"/>

			<StackPanel Orientation="Horizontal" Margin="0,0,0,10">
				<Button Content="Добавить" Width="100" Height="35" Click="AddButton_Click" Margin="0,0,10,0"/>
				<Button Content="Удалить" Width="100" Height="35" Click="DeleteButton_Click"/>
			</StackPanel>

			<DataGrid x:Name="AdsDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" Height="400" IsReadOnly="True" SelectionMode="Single">
				<DataGrid.Columns>
					<DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
					<DataGridTextColumn Header="Заголовок" Binding="{Binding Title}" Width="200"/>
					<DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
					<DataGridTextColumn Header="URL картинки" Binding="{Binding ImageURL}" Width="200"/>
					<DataGridTextColumn Header="Активна" Binding="{Binding IsActive}" Width="80"/>
				</DataGrid.Columns>
			</DataGrid>
		</StackPanel>
	</Grid>
</UserControl>
