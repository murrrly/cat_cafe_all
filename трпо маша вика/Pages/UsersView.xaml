<UserControl x:Class="RPM.Pages.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

	<Grid Margin="20">
		<StackPanel>
			<TextBlock Text="Сотрудники" FontSize="28" Margin="0,0,0,20"/>

			<StackPanel Orientation="Horizontal" Margin="0,0,0,10">
				<Button Content="Добавить" Width="100" Height="35" Click="AddButton_Click" Margin="0,0,10,0"/>
				<Button Content="Изменить роль" Width="120" Height="35" Click="ChangeRoleButton_Click" Margin="0,0,10,0"/>
				<Button Content="Блокировать" Width="100" Height="35" Click="BlockButton_Click"/>
			</StackPanel>

			<DataGrid x:Name="UsersDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" Height="400" IsReadOnly="True">
				<DataGrid.Columns>
					<DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
					<DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="200"/>
					<DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="100"/>
					<DataGridTextColumn Header="Роль" Binding="{Binding PositionName}" Width="150"/>
					<DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}" Width="80"/>
					<DataGridTextColumn Header="Регистрация" Binding="{Binding RegistrationDate}" Width="150"/>
				</DataGrid.Columns>
			</DataGrid>
		</StackPanel>
	</Grid>
</UserControl>
